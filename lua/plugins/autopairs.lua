return {
  "windwp/nvim-autopairs",
  event = "InsertEnter",
  opts = {
    -- enable_check_bracket_line = false -- Uncomment if you need it
  }
  -- No need for config function! Lazy calls setup(opts) automatically.
}
